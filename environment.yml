name: neural_mcts
channels:
  - pytorch
dependencies:
  - pytorch
  - networkx
  - tqdm
  - seaborn
  - pytest
  - pip
  - pip:
      - stable-baselines3
      - python-tsp
      - multiprocess
  - tensorboard

