name: neural_mcts
channels:
  - pytorch
dependencies:
  - pytorch
  - networkx
  - tqdm
  - seaborn
  - pip
  - pip:
      - stable-baselines3
      - python-tsp
      - multiprocess
  - tensorboard

